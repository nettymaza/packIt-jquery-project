<h2>Hi there  @<%= current_user.name %></h2>

<div class="links">
  <div class="">
    <%= link_to "View Trips", user_trips_path(current_user.id) %>
  </div>
  <div class="">
    <%= link_to "New Trip", new_user_trip_path(current_user.id) %>
  </div>
</div>
