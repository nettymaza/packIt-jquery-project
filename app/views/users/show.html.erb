<h2>Hi there  @<%= current_user.name %></h2>

<div class="trips">
    
</div>

<br>

<div class="links">
  <div class="">
    <%= link_to "Load Trips", user_trips_path(current_user.id), :class => "load_trips" %>
  </div>
  <div class="">
    <%= link_to "New Trip", new_user_trip_path(current_user.id) %>
  </div>
</div>
